There should only be ONE custom JS file.

Reasons why:
- Website is not big enough to need multiple custom JS files.
- Ensure only 1 GET request to load faster!!

When to change:
- When the website gets too big and the JS gets too huge.
